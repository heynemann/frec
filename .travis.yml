language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
# command to install dependencies
install: sudo aptitude install -y libevent-dev libxml2-dev curl libcurl3 libcurl4-openssl-dev && pip install -r test_requirements.txt --use-mirrors
# command to run tests
script: make ci_test
